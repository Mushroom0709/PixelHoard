# ── 必填 — 华为云 OBS ─────────────────────────────────
OBS_AK=E4PODEJ2KFRXLIBACJG5
OBS_SK=4G3TvKp0ehqOQPcDPOavWxiCgbid7fAGVxbjUMl5
OBS_ENDPOINT=obs.cn-central-221.ovaijisuan.com
OBS_BUCKET=obs-mushroom
OBS_WORKDIR=PixelHoard

# ── 必填 — 部署相关 ──────────────────────────────────
# ECS xj-ai-service
ECS_HOST=27.18.114.8
ECS_SSH_PORT=10332
ECS_USER=root
ECS_PASSWORD=kU2hVZdxaT4GBW8b
ECS_DEPLOY_DIR=/home/workspace/PixelHoard

# 业务对外端口(只暴露这一个)
PUBLIC_PORT=10388

# ── 必填 — 应用密钥 ──────────────────────────────────
# 用于 JWT 签名,生产环境务必替换
JWT_SECRET=please-change-me-in-prod-32chars-min
JWT_ACCESS_TTL_MIN=15
JWT_REFRESH_TTL_DAYS=30

# ── Admin bootstrap ──────────────────────────────────
# 首次启动 alembic upgrade head 时,如果 users 表为空,自动 seed 此 admin
ADMIN_EMAIL=admin@pixelhoard.com
ADMIN_PASSWORD=admin123!change-me
ADMIN_DISPLAY_NAME=PixelHoard Admin

# ── 数据库 ────────────────────────────────────────────
POSTGRES_USER=pixelhoard
POSTGRES_PASSWORD=pixelhoard-dev-pass
POSTGRES_DB=pixelhoard

# ── Redis ─────────────────────────────────────────────
REDIS_PASSWORD=

# ── CORS ──────────────────────────────────────────────
CORS_ORIGINS=http://localhost:5173,http://localhost:10388